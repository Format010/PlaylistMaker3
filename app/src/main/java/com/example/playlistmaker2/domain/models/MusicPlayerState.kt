package com.example.playlistmaker2.domain.models

enum class MusicPlayerState {
        STATE_DEFAULT,
        STATE_PREPARED,
        STATE_PLAYING,
        STATE_PAUSED,
        STATE_END_OF_SONG,
}